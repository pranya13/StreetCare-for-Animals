# StreetCare Frontend

Minimal React (CDN) app—no build step required.

## Run
Just open `index.html` in a browser (use a local server for CORS if needed).

- Backend default: `http://127.0.0.1:8000`
- Start backend first (see ../backend/README.md)

You can use a simple server:
```bash
# Python 3:
python -m http.server 5173
# then open http://127.0.0.1:5173
```
